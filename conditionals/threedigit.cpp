#include<iostream>
using namespace std;
int main(){
    int x;
    cout << "enter x: ";
    cin >> x;
    if(x>=100 && x<1000) cout << "x is three digit no";
    else cout << "x is not three digit no";
}